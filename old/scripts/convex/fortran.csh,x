#!/bin/csh -f
alias let @
let i = 1
while ( $i <= $#argv )
  set file = $argv[$i]
  switch ( $file )
    case *.for:
	set argv[$i] = ${file:r}.f
        ratty -s convex -I $MIRINC $file $argv[$i]
      breaksw
    default:
      breaksw
  endsw
  let i = $i + 1
end

fc $argv
