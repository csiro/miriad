$!*****************************************************************
$!		decnet_update.com
$!
$! This updates a foreign machine by copying the files to it.
$
$! Inputs:
$!  p1		File containing a list of files to be updated.
$!  p2		Name of the foreign machine.
$
$!  History:
$!    	       rjs	original version.
$!    20jul90  mchw	changed copy to copy/replace. Documentation.
$
$!*****************************************************************
$	status = 12
$	on control_y then goto 20
$	on error then goto 20
$	open/read updates 'p1'
$10:	read/end=15 updates file
$	in = f$element(0," ",file)
$	out = f$element(1," ",file)
$	set verify
$	copy/replace 'in' 'p2'::'out'
$	verf = 'f$verify("no")'
$	goto 10
$15:	status = 1
$20:	if f$logical("updates").nes."" then close updates
$	exit 'status'
