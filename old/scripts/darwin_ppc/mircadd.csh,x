#! /bin/csh -fe


setenv TMPDIR $MIR
foreach in ($argv)
 set dir = $in:h
 if ( "$dir" == "$in" ) set dir = "."
 cd $dir
 echo Processing $in
 switch ($in)
   case *.for:
   set tmp=$in:r
   set file = $tmp.f
   set out = $file:t
   set out = $out:r
   ratty -s linux -b -I $MIRINC $in $tmp.f
   f77 -ff90-intrinsics-hide -c -O -o ${out}.o $file
   ar r $MIRLIB/libmir.a ${out}.o
   rm -f $file ${out}.o
   breaksw
 case *.f2c:
   set tmp=$in:r
   set file=${tmp}.c
   set out=$file:t
   set out=$out:r
   intf2c -s linux $in $file;
   cc -O -c -o ${out}.o -I$MIRINC -I$MIR/subs -Dsun -Ddarwin $file
   ar r $MIRLIB/libmir.a ${out}.o
   rm -f ${out}.o $file
   breaksw
 case *.c:
   set out = $in:t
   set out = /tmp/$out:r
   cc -O -c -o ${out}.o -I$MIRINC -I$MIR/subs -Dsun -Ddarwin $in
   ar r $MIRLIB/libmir.a ${out}.o
   rm -f ${out}.o
   breaksw
 default:
   echo Ignoring $in
   breaksw
 endsw
end

exit 0
