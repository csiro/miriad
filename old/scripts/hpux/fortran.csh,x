#!/bin/csh -f
alias let @
let i = 1
while ( $i <= $#argv )
  set file = $argv[$i]
  switch ( $file )
    case *.for:
	set argv[$i] = ${file:r}.f
	flint -I $MIRINC $file
	ratty -s hpux -l -I $MIRINC $file $argv[$i]
      breaksw
    default:
      breaksw
  endsw
  let i = $i + 1
end

f77 $argv
